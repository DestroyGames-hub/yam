<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.yam.chat"
    android:versionCode="1"
    android:versionName="1.0">

    <!-- ===== İZİNLER ===== -->
    <!-- Ağ erişimi -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    
    <!-- Arka plan servisi -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    
    <!-- Push bildirimleri ve OneSignal -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.ACCESS_NOTIFICATION_POLICY" />
    
    <!-- Cihaz durumu -->
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    
    <!-- İsteğe bağlı izinler -->
    <uses-permission android:name="android.permission.CAMERA" android:required="false" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" android:required="false" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" android:required="false" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" android:required="false" />
    <uses-permission android:name="android.permission.READ_CONTACTS" android:required="false" />

    <application
        android:allowBackup="true"
        android:debuggable="false"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_launcher_round"
        android:supportsRtl="false"
        android:theme="@style/Theme.YAMChat"
        android:usesCleartextTraffic="false">

        <!-- ===== MAIN ACTIVITY ===== -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:screenOrientation="portrait"
            android:configChanges="orientation|screenSize|keyboardHidden"
            android:theme="@style/Theme.YAMChat">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <!-- URL Scheme -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="http" android:host="yourdomain.com" />
                <data android:scheme="https" android:host="yourdomain.com" />
            </intent-filter>
        </activity>

        <!-- ===== BACKGROUND SERVICE (24/7) ===== -->
        <!-- FCM/OneSignal Push Notification Servisi -->
        <service
            android:name=".services.YAMFirebaseMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <!-- OneSignal Bildirim Servisi -->
        <service
            android:name=".services.OneSignalNotificationService"
            android:exported="false" />

        <!-- Arka Plan Çalışma Servisi (Job) -->
        <service
            android:name=".services.BackgroundSyncService"
            android:exported="false"
            android:permission="android.permission.BIND_JOB_SERVICE" />

        <!-- Foreground Service (Görev Yöneticisinde Görünür Kal) -->
        <service
            android:name=".services.ForegroundMessagingService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <!-- ===== BROADCAST RECEIVERS ===== -->
        <!-- Cihaz açılışında servisi başlatma -->
        <receiver
            android:name=".receivers.BootReceiver"
            android:exported="true"
            android:permission="android.permission.RECEIVE_BOOT_COMPLETED">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
            </intent-filter>
        </receiver>

        <!-- Ağ durumu değiştiğinde -->
        <receiver
            android:name=".receivers.NetworkReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.net.conn.CONNECTIVITY_CHANGE" />
                <action android:name="android.net.wifi.WIFI_STATE_CHANGED" />
            </intent-filter>
        </receiver>

        <!-- OneSignal Bildirimleri -->
        <receiver
            android:name="com.onesignal.NotificationOpenedReceiver"
            android:exported="false" />

        <!-- ===== CONTENT PROVIDER ===== -->
        <!-- Service backup için -->
        <provider
            android:name=".provider.YAMChatProvider"
            android:authorities="com.yam.chat.provider"
            android:exported="false" />

        <!-- ===== META-DATA ===== -->
        <!-- OneSignal Konfigürasyonu -->
        <meta-data
            android:name="com.onesignal.app_id"
            android:value="ONESIGNAL_APP_ID_HERE" />

        <!-- Firebase Konfigürasyonu -->
        <meta-data
            android:name="com.google.firebase.ml.vision.DEPENDENCIES"
            android:value="barcode" />

    </application>

    <!-- ===== COMPATIBILITY ===== -->
    <uses-sdk
        android:minSdkVersion="24"
        android:targetSdkVersion="34" />

    <uses-feature
        android:name="android.hardware.screen.portrait"
        android:required="true" />

</manifest>
